[project]
name = "podpulse-backend"
version = "0.1.0"
description = "PodPulse Kubernetes monitoring backend"
requires-python = ">=3.10"
dependencies = [
    "fastapi>=0.115.0",
    "uvicorn>=0.34.0",
    "kubernetes>=36.0.0",
    "pydantic-settings>=2.0.0",
]
